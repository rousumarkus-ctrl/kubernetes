apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: image-claim
  namespace: project
spec:
  storageClassName: my-image-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

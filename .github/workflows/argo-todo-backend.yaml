name: todo-backend

on:
  push:
    branches:
      - '**'
    tags:
      - '!**'

env:
  USER: mrousu
  IMAGE: todo-backend
  FOLDER: ./the-project/todo-backend
  IMAGE_HOLDER: PROJECT/TODOBACKEND

jobs:
  build-publish:
    name: Build, Push, Release pingpong
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ env.FOLDER }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.ref }}

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Form the image name
        run: echo "IMAGE_TAG=$USER/$IMAGE:$GITHUB_SHA" >> $GITHUB_ENV

      - name: Build and publish backend
        run: |-
          docker build --tag "$IMAGE_TAG" .
          docker push "$IMAGE_TAG"

      - name: Set up Kustomize
        uses: imranismail/setup-kustomize@v2

      - name: Use right image
        run: kustomize edit set image $IMAGE_HOLDER=$IMAGE_TAG

      - name: commit kustomization.yaml to GitHub
        uses: EndBug/add-and-commit@v9
        with:
          add: 'the-project/todo-backend/kustomization.yaml'
          message: New version released ${{ github.sha }}

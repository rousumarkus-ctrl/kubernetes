apiVersion: apps/v1
kind: Deployment
metadata:
  name: pingpong-dep
  namespace: exercises
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pingpong
  template:
    metadata:
      labels:
        app: pingpong
    spec:
      #volumes:
      #  - name: shared-pong
      #    persistentVolumeClaim:
      #      claimName: pong-claim
      containers:
        - name: pingpong
          image: mrousu/pingpong:4b2b0277596a4692a5ed15877f800688e6d698e6
          env:
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_USER
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD
            - name: PGHOST
              value: postgres-svc
          #volumeMounts:
          #  - name: shared-pong
          #    mountPath: /usr/src/app/pongfiles

apiVersion: apps/v1
kind: Deployment
metadata:
  name: pingpong-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pingpong
  template:
    metadata:
      labels:
        app: pingpong
    spec:
      #volumes:
      #  - name: shared-pong
      #    persistentVolumeClaim:
      #      claimName: pong-claim
      containers:
        - name: pingpong
          image: mrousu/pingpong:eda9c36e404844fa635c75c293b44e114782452c
          #volumeMounts:
          #  - name: shared-pong
          #    mountPath: /usr/src/app/pongfiles
